# __APP_NAME__

## Bootstrap Context

This starter was generated by `apple-appdev-workflow:apple-app-bootstrap` using the Apple SwiftUI workflow.

## Baseline Assumptions

- Platform: __PLATFORM_LABEL__
- Deployment target: __DEPLOYMENT_TARGET__
- Architecture: composition-root driven dependency wiring with pure UI views
- Testing: Swift Testing for unit and integration tests
- Accessibility: first-draft baseline expected
- Concurrency: Swift 6 with strict concurrency enabled
- Layout mode: __LAYOUT_MODE__
- Local packages: __LOCAL_PACKAGES_SUMMARY__
- Starter UI: safe-area-aware, Dynamic Type-friendly, and adaptive across
  current iPhone and iPad sizes

## Starter Structure

- `App/Bootstrap`: composition root and app assembly
- `App/Features`: feature-facing UI
- `App/Services`: service protocols and live implementations
- `App/DesignSystem`: app-level visual tokens and shared styling
- `App/Support`: metadata and support files
- Harness handoff document: agent execution, validation, and follow-on lane
  guidance for the generated project
- `Tests`: starter Swift Testing coverage

## UI Baseline

The starter home surface is intentionally lightweight. It uses a scrollable
safe-area layout, relative SwiftUI text styles, reduced-motion-aware starter
animation, and grouped accessibility semantics so the first simulator launch
feels like a current Apple-platform baseline rather than a fixed-size demo.

__PACKAGE_GUIDANCE__

__GENERATED_DOCS_BLOCK__
