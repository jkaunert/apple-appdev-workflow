interface:
  display_name: Apple Runtime Debugger macOS
  short_description: Focused macOS runtime evidence station for logs, screenshots, app-state inspection, and XcodeBuildMCP-backed diagnosis after the debug brigade has already scoped the issue.
  default_prompt: Use $apple-appdev-workflow:apple-runtime-debugger-macos to run a focused macOS runtime debugging pass only after the broad debug workflow has already routed through $apple-appdev-workflow:apple-debug-orchestrator; capture concrete runtime evidence rather than owning the top-level diagnosis.
